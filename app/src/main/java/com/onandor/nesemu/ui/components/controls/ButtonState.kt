package com.onandor.nesemu.ui.components.controls

enum class ButtonState {
    UP, DOWN
}

enum class Button {
    DPAD_RIGHT,
    DPAD_LEFT,
    DPAD_DOWN,
    DPAD_UP,
    START,
    SELECT,
    B,
    A
}